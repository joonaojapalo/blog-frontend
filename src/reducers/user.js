//import * as types from '../constants/action-types'

const initialState = {username: 'JoonaO'}

// user  reducer
export default function (state = initialState , action) {
  switch (action.type) {
    default:
      return state
  }
}
